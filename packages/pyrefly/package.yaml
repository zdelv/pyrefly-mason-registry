---
name: pyrefly
description: Pyrefly is a type checker
homepage: https://github.com/facebook/pyrefly
licenses:
  - MIT
languages:
  - Python
categories:
  - Linter
  - LSP

schemas:
  # lsp: vscode:https://raw.githubusercontent.com/facebook/pyrefly/{{version}}/lsp/package.json
  lsp: vscode:https://raw.githubusercontent.com/facebook/pyrefly/refs/heads/main/lsp/package.json

source:
  id: pkg:pypi/pyrefly@0.15.2

bin:
  pyrefly: pypi:pyrefly

# neovim.lspconfig *must* be used if an lspconfig file exists for pyrefly in
# the nvim-lspconfig repo (which it does not).
# neovim:
#   lspconfig: pyrefly
